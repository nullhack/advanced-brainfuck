[project]
name = "advanced-brainfuck"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
brainfuck = "brainfuck:main"

[dependency-groups]
dev = [
    "agents-smith>=1.0.0",
]
